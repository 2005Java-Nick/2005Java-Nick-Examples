import { CardValuePipe } from './card-value.pipe';

describe('CardValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CardValuePipe();
    expect(pipe).toBeTruthy();
  });
});
